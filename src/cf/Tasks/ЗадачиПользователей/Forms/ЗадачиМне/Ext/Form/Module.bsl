
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Список.Параметры.УстановитьЗначениеПараметра("Выполнена", Выполнена);
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВыполненные(Команда)
	Выполнена = не Выполнена;
	Список.Параметры.УстановитьЗначениеПараметра("Выполнена", Выполнена);
	Элементы.Список.Обновить();
	Элементы.ФормаПоказатьВыполненные.Заголовок = ?(Выполнена,"Показать невыполненные", "Показать выполненные")
КонецПроцедуры
